{
  "name": "webapp.tests",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "playwright test && npx playwright show-report",
    "test:chromium": "playwright test --project=\"Google Chrome\" --headed",
    "posttest:requirements": "playwright test ./tests/Requirements.spec.ts --project=\"Google Chrome\" --headed",
    "posttest:projects": "playwright test ./tests/Projects.spec.ts --project=\"Google Chrome\" --headed",
    "start": "npm run test:chromium"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.1"
  }
}
